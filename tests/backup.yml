# Keepass database
database_path: tests/test.kdbx
# Test backup configuration
profiles:
  test-backup:
    type: borg
    title: borg
    input:
      - tests/dir
    output: tests/borg
    exclude:
      - tests/dir/excludeme
    must_exist:
      - tests/dir/README.md
